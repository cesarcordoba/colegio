.busqueda
    autocomplete
.noticia-container
    .noticia-content
        .barra
            button(mat-button) TODO
            .linea
            button(mat-button) INSTITUCION
            .linea
            button(mat-button) ACTUALIDAD
            .linea
            button(mat-button) PRENSA
        .grid-noticias
            .grid-item-noticia(*ngFor="let noticia of noticias")
                .etiqueta([style.backgroundColor]="noticia.color")
                    p {{noticia.tipo}}
                .imagen
                    img([src]="noticia.portada[0].link")
                .texto
                    p {{noticia.introduccion}}
                    a(mat-button (click)="mandarNoticia(noticia.id)") Leer mas
